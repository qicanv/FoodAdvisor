<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foodadvisor.mapper.DishMapper">

    <select id="selectByMerchantId" resultType="com.foodadvisor.entity.Dish">
        SELECT * FROM dishes 
        WHERE merchant_id = #{merchantId}
          AND status = 'ACTIVE'
          AND deleted_at IS NULL
        ORDER BY recommended DESC, created_at DESC, id ASC
    </select>

    <select id="selectActiveByMerchantIds" resultType="com.foodadvisor.entity.Dish">
        SELECT *
        FROM dishes
        WHERE merchant_id IN
        <foreach collection="merchantIds" item="merchantId" open="(" separator="," close=")">
            #{merchantId}
        </foreach>
          AND status = 'ACTIVE'
          AND deleted_at IS NULL
        ORDER BY merchant_id ASC, recommended DESC, created_at DESC, id ASC
    </select>

</mapper>
